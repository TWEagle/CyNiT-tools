# Organization-wide constraints / baselines
# Keep these loose unless you have a strong reason to pin.

Flask>=3.1,<3.2
cryptography>=44
Pillow>=10

# NOTE:
# - Do NOT add python-msilib. It is not available as a pip distribution on Windows.
